# Core homomorphic-encryption engine
tenseal==0.3.16
numpy>=1.24            # required by TenSEAL

# Server + web dashboard (server.py / server_side/app.py)
fastapi==0.138.1
uvicorn==0.49.0
pydantic==2.13.4

# Agent: client that calls the server (agent.py)
requests>=2.31

# Agent planner (agent_side/planner.py) - Azure OpenAI
openai>=1.0

# Plotting benchmark results
matplotlib>=3.8